1. If Operator is of 2 character , it don't works ✅
        Eg: == ->    = -> Equal & = -> Equal
        But it should be == -> isEqual
    & Similarly => GreaterEqual
2. It Doesn't support Decimal floating Value
        Eg : 12.5 ->    12 -> Number & . -> Unknown & 5 -> Number
        But it should be 12.5 -> Number
3. I also don't implement the comment in lexer
4. 